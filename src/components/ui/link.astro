---
interface Props {
  href: string;
  size?: "md" | "lg";
  block?: boolean;
  style?: "outline" | "primary" | "inverted" | "muted";
  class?: string;
  [x: string]: any;
}

const {
  href,
  block,
  size = "lg",
  style = "primary",
  class: className,
  ...rest
} = Astro.props;

const sizes = {
  lg: "px-5 py-2.5",
  md: "px-4 py-2",
};

const styles = {
  outline: "bg-theme border-2 border-theme hover-bg-theme-surface text-theme",
  primary: "bg-theme-primary text-theme-on-primary hover-bg-theme-primary border-2 border-transparent",
  inverted: "bg-theme text-theme border-2 border-transparent",
  muted: "bg-theme-surface hover-bg-theme-surface border-2 border-transparent text-theme",
};
---

<a
  href={href}
  {...rest}
  class:list={[
    "rounded-sm text-center transition focus-visible:ring-2 ring-offset-2 ring-gray-200",
    block && "w-full",
    sizes[size],
    styles[style],
    className,
  ]}
  ><slot />
</a>
