---
import RichText from "@/components/page-builder/rich-text.astro";
import SponsorGroups from "@/components/sponsors/sponsor-groups.astro";
import { getPublishedSponsors, groupSponsorsByType } from "@/lib/sponsors";

const { title, body, types = [] } = Astro.props;

const sponsors = await getPublishedSponsors();
const filteredSponsors =
  types.length > 0
    ? sponsors.filter((sponsor) => types.includes(sponsor.data.type))
    : sponsors;
const sponsorGroups = groupSponsorsByType(filteredSponsors);
---

<section class="py-12">
  <div class="mx-auto max-w-6xl">
    <div class="max-w-3xl">
      <h2 class="text-4xl font-bold tracking-tight text-foreground lg:text-5xl">
        {title}
      </h2>
      {body && <RichText content={body} class="mt-4 text-muted-foreground" />}
    </div>

    <div class="mt-12">
      {
        sponsorGroups.length > 0 ? (
          <SponsorGroups groups={sponsorGroups} />
        ) : (
          <p class="text-muted-foreground">No sponsors match the selected types yet.</p>
        )
      }
    </div>
  </div>
</section>
